general:
  branches:
    only:
      - master
machine:
  node:
    version: 7.3.0
dependencies:
  pre:
    - npm install yarn babel-cli eslint -g;
    - yarn install;
    - yarn lint;
    - yarn flow;
    - yarn test:coverage;
