general:
  branches:
    only:
      - master
machine:
  node:
    version: 7.3.0
dependencies:
  pre:
    - npm install yarnpkg babel-cli -g;
    - yarn lint;
    - yarn flow;
    - yarn test:coverage;
